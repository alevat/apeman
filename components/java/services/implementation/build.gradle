dependencies {
    apt "org.mapstruct:mapstruct-processor:${mapstruct_version}"

    compile project(':components:java:services:apeman-services-api')
    compile project(':components:java:apeman-persistence')

    compile "org.mapstruct:mapstruct-jdk8:${mapstruct_version}"
}