import ProjectModule from './project.module';

describe('Service ProjectService', () => {
    it('should be able to produce output in the test', () => {
        console.info("Hello from the test!");
    });
});